/// <reference types="react" />
import './inject';
interface Props {
    radius: number | string;
    color: string;
    stroke: number | string;
    visible: boolean;
    [key: string]: any;
}
declare const Spinner: ({ visible, color, stroke, radius, ...rest }: Props) => JSX.Element | null;
export default Spinner;
