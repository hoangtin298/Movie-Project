import * as React from 'react';
import { GridColDef } from '../../models/colDef/index';
import { GridOptions } from '../../models/gridOptions';
import { GridSortDirection } from '../../models/gridSortModel';
interface GridColumnHeaderItemProps {
    colIndex: number;
    column: GridColDef;
    isDragging: boolean;
    isResizing: boolean;
    sortDirection: GridSortDirection;
    sortIndex?: number;
    options: GridOptions;
    filterItemsCounter?: number;
    hasFocus?: boolean;
    tabIndex: 0 | -1;
}
export declare const GridColumnHeaderItem: React.MemoExoticComponent<({ column, colIndex, isDragging, isResizing, sortDirection, sortIndex, options, filterItemsCounter, hasFocus, tabIndex, }: GridColumnHeaderItemProps) => JSX.Element>;
export {};
